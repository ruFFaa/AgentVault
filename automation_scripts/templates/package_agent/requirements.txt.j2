# Core dependencies (managed by pyproject.toml, but good for reference/docker)
fastapi>=0.111,<0.112
uvicorn[standard]>=0.29,<0.35
agentvault-server-sdk{{ sdk_version_req }}

# Add other specific dependencies for your agent below
# e.g., httpx, numpy, pandas, etc.
